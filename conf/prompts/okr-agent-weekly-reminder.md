# 周报催填

目标：在行动指定的范围内检查当前周未填写的 KR，并只向应负责且可触达的人发出一次可审计提醒。

读取行动目标、`action_scope` 和 `action_recipient`，再读取 `weekly-report-reminder` Skill。行动对象描述的是收件规则或目标群体，不是已经确认的身份；发送前必须解析并回读真实 open_id，不能按姓名猜测。按负责人聚合缺失项，先生成催填批次，再由 M5 根据具体消息和目标判断审批。

没有缺失项时直接结束，不制造通知。完成时报告检查范围、缺失数量、实际收件人、逐人发送回执、跳过原因和无法解析的对象。
