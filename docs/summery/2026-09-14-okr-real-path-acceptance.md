# OKR 真实路径验收记录（2026-09-14）

测试目标：用 `chujiejie.1 / 储节节` 现有飞书授权，验证公开主站 `https://emily.bytedance.net` 的评论、导出、人员和 @ 路径。执行时 checkout 为 `b770f65d`，浏览器及飞书读回原始 JSON 保存在本机未入库的 `var/okr-real-run.*` 和 `/tmp/okr-real-*.json`。

测试程序先从本机保存的授权读取令牌，实时调用飞书 `authen/v1/user_info`，确认姓名、应用内 open_id 与 union_id 均为储节节，再向目标服务会话库签发 45 分钟的专用会话。公开域名的 `/api/biz-okr/me` 确认该会话为储节节；页面和业务 API 未做 mock、拦截或改写。测试结束调用产品登出接口撤销会话。**此方法覆盖登录后的真实业务路径，不覆盖设备授权登录步骤。**

| 用例 | 真实执行结果 |
|---|---|
| Review／周报／会议／Plan 导航 | 五个页面均保持储节节身份，没有落入登录页；通过 |
| 人员目录 | 搜索储节节，返回完整企业邮箱；重复搜索结果一致；通过 |
| Review 评论 | 发布、作者归属、立即显示、刷新回读、回复、编辑、解决、历史、重开、数据库回读；通过 |
| 周报评论 | 同上；通过 |
| 临时 Plan | 新建 Plan、目标、KR，修改优先级、选择本人 Owner、刷新回读，并完成同样的评论流程；通过 |
| Review／周报导出 | 页面按钮各创建一篇飞书文档；飞书云盘元数据确认两篇的 `owner_id` 均为储节节，文档正文分别回读 58,103 和 50,840 字符；通过 |
| @ 本人 | 评论中选中本人，通知机器人实际发出卡片；飞书消息 ID 可回读，会话成员为储节节；送达通过 |
| @ 卡片跳转 | 主站评论生成的卡片指向 `https://emily.bytedance.net/dev/#/weekly-report?...`，落到开发预览入口；**未通过**。消息送达不等于跳转正确 |

实际导出文档：[2026-W36 OKR Review](https://bytedance.my.larkoffice.com/docx/CZCrd1wg9o3QdixOYgmmle8Hyth)、[2026-W35 OKR 周报会议](https://bytedance.my.larkoffice.com/docx/Fql7dzHx0o3ELnxw5RymT6HNyab)。测试没有扩大其默认分享权限。@ 消息 ID 为 `om_x100b654a5c27f0acc196e2be2560afc`，发件应用为 Jarvis通知机器人 `cli_a96a2422f03bdbd7`。

所有本轮临时评论与 Plan 已按自身 ID 删除；通知测试留下的孤立投递审计行也按评论、邮箱及飞书消息 ID 精确清理，飞书已发出的测试卡片仍可供核查。正式 OKR 内容没有作为测试目标修改。首次 Plan 脚本在新建 API 返回后立即尝试新建目标，暴露了“API 已返回但页面还未切换到新 Plan”的时序；脚本现在必须等“选择 Plan”的值等于新 Plan ID 才继续写。测试期间误建在原 Plan 的一条带运行编号的目标已精确删除，并回读确认本轮残留评论和 Plan 均为零。此处的 UI 时序风险仍值得在产品交互中单独处理。

尚未真实验收：产品设备登录流程、第二个真人账号间的数据隔离、授权撤销／过期的真实故障、图片与附件、区域对齐和 Agent 动作。现有 Go/TypeScript 模拟测试保留为快速回归，不计入上述真实路径结果。`scripts/okr-real-acceptance` 会分别执行浏览器和飞书外部回读；卡片跳转等任一结果失败时返回非零。
