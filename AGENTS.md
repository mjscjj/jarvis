# AGENTS 项目规范

给在本仓库工作的 AI agent 的约定。写代码前先读，动手时遵守。完整设计见 `[docs/00-overview.md](docs/00-overview.md)`，架构导航见 `[README.md](README.md)`。

## 0. 项目目标与设计哲学（纲领，先读）

Jarvis 是"我"的**个人助手**，跑在本地可信环境。它的本质不是"规则引擎填表单"，而是一个**靠上下文推断、能自己用工具查信息来补全判断**的 agent。所有模块设计都要服从下面这条主线。

### 0.1 一句话目标

把飞书里的一句话——无论是 **leader 交办给我的**（"你去读下 agent runtime 项目的 agent loop 代码"），还是**我自己说的**（"我回去读下 agent runtime 的 agent loop 代码"）——连同它产生时的全部上下文（谁说的、在哪个群、群绑定什么项目、群公告、项目背景、相关记忆……），一路推断、补全、传递，最终变成一个**上下文完整、目标明确的任务**，交给执行者做完。

### 0.2 核心原则：上下文全程携带，缺什么让模型自己去查

1. **上下文一次组装、全程传递、逐字复用**
  线索产生时的上下文在 M3 抽取阶段一次性组装并**冻结成快照**（`Todo.context_snapshot`），Todo → Task → 执行者全链路复用同一份快照，不在下游重新拼凑。上下文可以**压缩/总结**，但**不能丢**——执行者拿到手的任务必须自带"它是谁、为什么、属于哪个项目、代码在哪"这些信息。
2. **信息优先靠推断和工具自查，而不是让用户填**
  Jarvis 是助手，不是表单。很多信息模型**可以从上下文推断出来**（这条是谁交办的、属于哪个项目），或者**可以用工具查出来**（`jarvis-tools` 查项目/人/群、`lark-cli`/`bytedcli` 查飞书侧公告/成员/文档、`git` 查仓库）。**能推断、能查到的，就不要问用户**。默认让大模型运行在 `danger-full-access`+联网环境里，自己动手找证据、找答案。
3. **推断链路的两个关键推算点**
  - **项目归属推算**：群绑定了项目 → 直接用项目信息；没绑定 → 模型用决策工具查（当前项目有哪些、群公告说了什么、发起人在哪些项目），推断出属于哪个项目。
  - **代码地址推算**：code_change 类任务，先看任务显式的 `repo_ref`，否则从"绑定项目的代码资源（repos）"里找到本地仓库路径。推算出来的仓库随任务一起交给执行者。
4. **实在推断不出来，才回退问用户**
  自动补全是首选，不是唯一手段。当模型用尽工具仍拿不到关键上下文时，才产出"请你补充 XX"的诉求，交回给用户。



### 0.3 端到端链路（目标形态）

```
飞书消息（谁说的 / 在哪个群 / 群绑定项目 / 群公告 / 项目背景 / 相关记忆 全部在场）
   │  M3 抽取：上下文一起进 prompt；模型可自跑工具推算项目归属
   ▼
Todo（附带冻结的上下文快照 context_snapshot，随库落地）
   │  M4 决策（need_info 的四种出路，任选其一）：
   │    ① 用工具进一步【自动补全】缺失上下文
   │    ② 补不到 → 产出【要求用户补充上下文】的 Todo
   │    ③ 上下文已足够 → 【直接产出可执行结果/放行】
   │    ④ 判断不值得做 → 【什么都不做】
   ▼
Task（据 Todo 上下文推算：属于哪个项目、代码仓库地址在哪——推算 or 从绑定项目 repos 里找）
   │  M5 执行：带着完整、明确的上下文交给执行者；上下文可压缩总结但都在
   ▼
执行者（codex agent，本地可信环境自跑 lark-cli/bytedcli/git/jarvis-tools 真正做完）
```



### 0.4 给决策/执行模型的工具体系

Jarvis 为模型提供一批**只读决策工具**（`cmd/jarvis-tools`，输出 JSON 供稳定解析），让模型无需追问就能查到 Jarvis 自有数据：当前有哪些项目、项目进度/背景、我的 leader 是谁、同事有哪些、某位同事与"我"的关系、群绑定与群公告、会话历史等。工具集应随需要**持续扩充**，方向是"凡是模型判断时需要、又能从系统里查到的信息，都给它一个工具"。除自有工具外，模型还可直接使用 `lark-cli`/`bytedcli`/`git` 等外部 CLI 补充飞书侧与代码侧信息。

### 0.5 流水线必须通用：不为特定来源开专用链路

M2 → M3 → M4 → M5 是**一条通用流水线**，每一段只有一套协议。来源的差异（飞书群消息、单聊、会议妙记、邮件、日程、外部系统……）**只能通过提示词、Skill 和工作规则表达，不能在 Go 里开一条专用链路**。

- **M2 只做机械采集。** 去外部世界取东西，把**原始事实**（成功的原始返回、失败的原始错误全文）写成中立证据。不分类、不下结论、不判断"要不要重试"、不按固定长度截断内容。
- **M3/M4/M5 只有一套协议。** 不为某个来源新增状态枚举、专用表、专用分支，也不为它开校验豁免。
- **判断归模型。** "这场会有没有开录制""妙记是还没生成好还是真的没权限""该不该再等一会儿"——这类都是结合上下文的语义判断，写进提示词让模型判，不写成 Go 的 `if`、常量或字符串匹配。
- **等待与重试归 M5。** 需要等未来条件时用 `jarvis-tools yield-until` 挂起当前 Task，由模型决定等多久、等几次、何时放弃；不在采集层写 `retryDelay` 常量和重试上限。

反面例子（均已在本仓库发生过，勿重犯）：

- 用 `strings.Contains(err, "permission")` 判断错误语义——把"没录制 / 还没生成好 / 真的没权限"三种情况压成一种，导致 M3 产出假线索。
- 用 `waiting_minutes` / `permission_denied` 这类状态枚举，把"为什么还没拿到"固化进 Go。
- 用 `if 产物 A 为空才去取产物 B` 替模型决定该取哪些产物。
- 采集期按固定字符数截断证据：原文已经落盘，却只把残缺副本交给下游。
- 在 M3 校验里按来源开豁免分支（如"证据全部来自会议时跳过 assigner 校验"）。

接入一个新来源的正确做法：确认它能落进现有的证据协议 → 在提示词或 Skill 里写清这个来源的判断要点 → **不写 Go**。

具体到"某个来源怎么进 M2"：用**线索投递**这个唯一通用入口，`POST /api/clues` / `jarvis-tools append-clue`。写一个定时任务 + Skill，让 agent 自己去外部世界取事实，再把事实原样投递回来。M2 收下后只做三件机械动作：存进 `message`、按 `(source, external_id)` 幂等、唤醒 M3。**新来源等于一个新 `source` 值加一份 Skill，不等于一个新 Go 模块。**



## 1. 早期优先 MVP，不把功能做复杂

- 早期阶段一切以**跑通 MVP 功能**为先，选最简单能满足需求的实现，不做过度设计、不提前抽象、不为想象中的未来需求预留复杂结构。
- 单用户、本地、低频系统——避免分布式、缓存、插件化、复杂并发等与当前规模不匹配的方案。
- 如果某个需求的合理实现**确实比较复杂**（涉及架构改动、多模块联动、明显的性能/一致性权衡），**先停下来和用户确认**，讲清复杂点和更简单的替代方案，由用户决定，不要自行铺开。
- **不用数据库事务**：单用户、本地、低频，不为多写操作套 `Transaction`/回滚。多步写入按顺序直接写，中途出错就 fail-fast 报错退出、下次重跑，不追求跨步骤的原子一致性。如某处确有强一致需求，先和用户确认。

## 2. Agent-first：只存骨架，语义交给模型

设计存储、接口、状态和流程时，先问一句：**这件事必须由程序保证，还是模型能结合上下文判断？** 只有必须由程序保证的内容，才设计成固定字段和约束。

### 2.1 强制：大模型语义默认使用宽松结构

凡是进入或来自大模型的提示词、上下文、目标、计划、判断、原因、证据、过程和结果，**默认使用完整自然语言或宽松 JSON，禁止无必要地定义成强约束结构**。让模型结合上下文理解和推断，不要把模型能处理的语义提前固化成程序协议。

- **不要为模型语义复制一套固定 DTO。** 如果一段内容只是整体写入 Prompt、整体持久化、整体传给下一阶段，直接保留原始 `TEXT` / `json.RawMessage`；不要仅为了序列化而新增层层 Go struct、必填字段和 schema 版本。
- **不要提前封闭模型的表达空间。** 非硬控制字段不要随意增加枚举、`additionalProperties: false`、必填键、固定嵌套层级、原因码或类型分支；新增语义内容应允许直接扩展 JSON，而不要求同步修改全链路结构体。
- **结构化只服务程序硬消费。** 只有字段确实被程序用于 SQL 查询/索引、关联、幂等、调度、权限、安全审批、状态流转或执行参数时，才允许建立固定字段或严格 schema。必须能指出具体消费代码；说不出消费点，就保持宽松。
- **最小投影，不丢原文。** 程序需要某个控制值时，只投影该最小字段，原始自然语言或 JSON 仍完整保留并继续交给模型。不要用投影字段替代、删减或重写原始上下文。
- **严格协议只包硬边界。** 工具调用参数、执行状态、审批动作、幂等键等机器协议可以严格校验；协议中的目标、背景、理由、计划正文和执行结果仍应保留为宽松文本或 JSON。
- **阶段之间必须尽量解耦。** M3、M4、M5 只共享 ID、关联、状态、版本等最小控制字段，以及完整原始语义 JSON；下游不要依赖上游内部 DTO、枚举或字段清单。上游新增一段模型语义时，下游应能直接收到并交给模型，不应被迫同步修改结构体和校验器。
- **每个阶段只约束自己的硬边界。** 阶段内部可以为当前操作建立最小局部类型，但不要把局部实现提升成全链路公共大结构。阶段交接优先采用“稳定小外壳 + 宽松 payload”；只有确实由下游程序执行的参数，才定义严格跨阶段契约。
- **禁止下游重建上游语义。** 上游已经组装的上下文、推断依据和原文必须原样传递；下游可以补充新信息，但不能重新查库拼一份“看起来等价”的背景，也不能把宽松内容重新拆成一批固定字段后再传递。
- **新增强结构前必须先论证。** 增加模型相关的数据库列、Go struct 字段、JSON Schema 必填项、枚举或 validator 前，必须先说明它解决的真实不稳定问题、程序消费点，以及为什么让模型推断不够；没有这些证据就不要增加。

- **字段要薄。** 通常只保存 ID、关联关系、时间、版本、少量控制状态，以及权限、幂等、调度等必要字段。
- **语义不要定死。** 目标、计划、子目标、原因、阻塞、判断过程和结果，优先保存为完整的自然语言 `TEXT` 或宽松 `JSON`，交给模型理解和更新。
- **不要提前枚举模型的判断。** 不要因为代码容易处理，就把语义拆成大量类型、布尔值、原因码、状态流转和规则表。
- **需要时再投影字段。** 只有某项信息确实需要稳定查询、索引、调度或强制校验时，才从语义内容中增加一个结构化字段。
- **约束只守硬边界。** 权限、幂等、并发、调度和不可逆操作由代码保证；其余判断优先交给模型。
- **上下文要完整。** 保留原文、来源和上下文快照，让模型基于完整事实推断，不要求上游把所有语义预先结构化。

例如，设计 Goal 时可以先只保留 `objective`、`status`、关联 ID 和版本等关键字段。当前计划、子目标、调整原因和进度可以由模型写在一段可更新的语义内容中；不要一开始就为它们建立复杂表结构和状态机。

发现模型确实无法稳定处理某个问题后，再增加字段或约束。不要为想象中的问题提前设计复杂系统，也不要为错误抽象增加兼容层或 fallback。

## 3. 简单文本配置统一使用本地 Markdown

系统提示词、模板、说明、策略正文等“通过一个稳定 key 读写一段文本”的简单配置，统一放在配置文件同级的 `prompts/` 目录。文件是唯一真源，**不要写入数据库，也不要在代码里复制正文作为 fallback**。

- **文件注册**：在 `internal/textstore/defaults.go` 注册固定 key、展示名和文件名。只允许访问注册文件，不接受任意路径。
- **服务入口**：后端使用 `internal/textstore.Service`。运行时依赖 `textstore.Reader`，通过 `Content(ctx, key)` 实时读取；文件缺失或正文为空直接报错。
- **管理接口**：管理端复用 `/api/text-files` 的 list/get/update。系统依赖文件不允许从后台创建或删除。
- **Prompt 分层**：文件中的系统提示词只描述 Agent 的角色、目标和稳定行为。工具说明由 `internal/toolcatalog` 和 Skills 维护；阶段标识、上下文、审批产物和 JSON Schema 由运行时代码动态组装。
- **什么时候才建表**：只有数据确实需要结构化字段、关联关系、索引查询、独立生命周期或硬状态约束时才建表。

新增简单文本配置的标准步骤：

1. 在 `internal/textstore/defaults.go` 注册稳定 key、展示名和文件名。
2. 在 `conf/prompts/` 提交对应 Markdown 正文。
3. 业务运行时通过注入的 `textstore.Reader.Content` 读取，不加静默 fallback。
4. 如需后台编辑，前端直接调用通用 `text-files` API，并固定该业务 key。
5. 测试至少覆盖读写、未知 key 拒绝、缺失/空正文 fail-fast。

## 4. 其它既有约定

- 代码风格 fail-fast：暴露问题而非掩盖，尤其单测；不乱加兜底 fallback。
- 展示列表优先行内编辑，见 `[.cursor/rules/list-inline-edit.mdc](.cursor/rules/list-inline-edit.mdc)`。
- 写组件/代码前优先复用已有官方包和仓库内已有实现。
- 构建或重启主服务必须执行 `./scripts/rebuild-server.sh`；禁止裸 `go build` 覆盖 `bin/jarvis-server` 后直接 `launchctl kickstart`，否则会破坏 macOS TCC 稳定签名。
