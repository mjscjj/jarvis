# AGENTS 项目关键原则

给在本仓库工作的 AI agent 的最小常驻约束。详细规范、语义所有权表、反例和操作说明统一见 [README.md 的 AI Agent 详细开发规范](README.md#ai-agent-详细开发规范)；项目愿景见 [goal.md](goal.md)，完整架构见 [docs/00-overview.md](docs/00-overview.md)。

1. **先判语义所有权，再改内容。** 沿生产者、组装器、消费者和持久化真源还原最终生效链路；一个语义只保留一个权威来源，优先删除或迁移错误真源，不做下游反向补丁。
2. **Agent-first，只把机器硬边界写进代码。** 判断、编排、调查和审批尺度放在对应阶段的 prompt、rules 或 Skill；代码只保证状态、引用、幂等、调度、参数和执行留痕等必须稳定的约束。
3. **当前是本地单用户可信 MVP，不建设 Agent 内部权限。** 各阶段默认可使用全部工具；stage、actor、Task ID 等用于上下文和留痕而非鉴权。阶段职责由提示词约束，审批由 M5 结合具体动作判断。
4. **上下文一次组装、完整传递，调查深度服从阶段职责。** M2 原样采集；M3 只补足准入所需的最短证据；M5 围绕真实目标深入调查、执行和验证。完整原文不能丢，下游不得重建等价背景替代冻结现场。
5. **保持唯一通用流水线。** 新来源走统一 clue 入口，以新的 source 和 Skill 表达差异；不得为会议、邮件、群聊等来源增加 Go 专用分支、状态枚举、重试链路或校验豁免。
6. **M5 是执行期决策者。** `Task.source_payload` 是证据而非不可修改的合同；M5 可根据新事实调整目标和动作。是否请示 principal 不按 `action_type` 判断，但所有外部副作用必须如实记录。
7. **MVP、宽松语义和 Markdown 优先。** 选择系统概念最少的方案；模型产生和消费的目标、背景、判断、计划与结果使用完整自然语言或宽松 JSON。只有程序确需查询或约束时才增加结构化字段；稳定文本配置以仓库 Markdown 为唯一真源并 fail-fast。
8. **完成最小语义闭包并验证。** 同步真源、组装点、消费者、文档和必要测试，保留用户已有改动，不顺手重构。构建或重启主服务必须使用 `./scripts/jarvis-deploy --skip-pull`；重要架构与正式交付材料统一放在 `docs/summery/`。临时调查、生成、回读和回执建议放在 `~/tmp/jarvis/`；模块持久化真源（如 `data/okr/`）、用户指定路径和正式交付仍使用各自权威目录。

- **OKR 产品数据必须提交**：`data/okr/okr.db` 及 `data/okr/` 下新增或修改的产品资源必须随改动提交。
