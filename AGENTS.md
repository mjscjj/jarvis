# AGENTS 项目规范

给在本仓库工作的 AI agent 的约定。写代码前先读，动手时遵守。完整设计见 `[docs/00-overview.md](docs/00-overview.md)`，架构导航见 `[README.md](README.md)`。

## 0. 项目目标与设计哲学（纲领，先读）

Jarvis 是"我"的**个人助手**，跑在本地可信环境。它的本质不是"规则引擎填表单"，而是一个**靠上下文推断、能自己用工具查信息来补全判断**的 agent。所有模块设计都要服从下面这条主线。

### 0.1 一句话目标

把飞书里的一句话——无论是 **leader 交办给我的**（"你去读下 agent runtime 项目的 agent loop 代码"），还是**我自己说的**（"我回去读下 agent runtime 的 agent loop 代码"）——连同它产生时的全部上下文（谁说的、在哪个群、群绑定什么项目、群公告、项目背景、相关记忆……），一路推断、补全、传递，最终变成一个**上下文完整、目标明确的任务**，交给执行者做完。

### 0.2 核心原则：上下文全程携带，缺什么让模型自己去查

1. **上下文一次组装、全程传递、逐字复用**
  线索产生时的上下文在 M3 抽取阶段一次性组装并**冻结成快照**（`Todo.context_snapshot`），Todo → Task → 执行者全链路复用同一份快照，不在下游重新拼凑。上下文可以**压缩/总结**，但**不能丢**——执行者拿到手的任务必须自带"它是谁、为什么、属于哪个项目、代码在哪"这些信息。
2. **信息优先靠推断和工具自查，而不是让用户填**
  Jarvis 是助手，不是表单。很多信息模型**可以从上下文推断出来**（这条是谁交办的、属于哪个项目），或者**可以用工具查出来**（`jarvis-tools` 查项目/人/群、`lark-cli`/`bytedcli` 查飞书侧公告/成员/文档、`git` 查仓库）。**能推断、能查到的，就不要问用户**。默认让大模型运行在 `danger-full-access`+联网环境里，自己动手找证据、找答案。
3. **推断链路的两个关键推算点**
  - **项目归属推算**：群绑定了项目 → 直接用项目信息；没绑定 → 模型用决策工具查（当前项目有哪些、群公告说了什么、发起人在哪些项目），推断出属于哪个项目。
  - **代码地址推算**：code_change 类任务，先看任务显式的 `repo_ref`，否则从"绑定项目的代码资源（repos）"里找到本地仓库路径。推算出来的仓库随任务一起交给执行者。
4. **实在推断不出来，才回退问用户**
  自动补全是首选，不是唯一手段。当模型用尽工具仍拿不到关键上下文时，才产出"请你补充 XX"的诉求，交回给用户。



### 0.3 端到端链路（目标形态）

```
飞书消息（谁说的 / 在哪个群 / 群绑定项目 / 群公告 / 项目背景 / 相关记忆 全部在场）
   │  M3 抽取：上下文一起进 prompt；模型可自跑工具推算项目归属
   ▼
Todo（附带冻结的上下文快照 context_snapshot，随库落地）
   │  M4 决策（need_info 的四种出路，任选其一）：
   │    ① 用工具进一步【自动补全】缺失上下文
   │    ② 补不到 → 产出【要求用户补充上下文】的 Todo
   │    ③ 上下文已足够 → 【直接产出可执行结果/放行】
   │    ④ 判断不值得做 → 【什么都不做】
   ▼
Task（据 Todo 上下文推算：属于哪个项目、代码仓库地址在哪——推算 or 从绑定项目 repos 里找）
   │  M5 执行：带着完整、明确的上下文交给执行者；上下文可压缩总结但都在
   ▼
执行者（codex agent，本地可信环境自跑 lark-cli/bytedcli/git/jarvis-tools 真正做完）
```



### 0.4 给决策/执行模型的工具体系

Jarvis 为模型提供一批**只读决策工具**（`cmd/jarvis-tools`，输出 JSON 供稳定解析），让模型无需追问就能查到 Jarvis 自有数据：当前有哪些项目、项目进度/背景、我的 leader 是谁、同事有哪些、某位同事与"我"的关系、群绑定与群公告、会话历史等。工具集应随需要**持续扩充**，方向是"凡是模型判断时需要、又能从系统里查到的信息，都给它一个工具"。除自有工具外，模型还可直接使用 `lark-cli`/`bytedcli`/`git` 等外部 CLI 补充飞书侧与代码侧信息。



## 1. 早期优先 MVP，不把功能做复杂

- 早期阶段一切以**跑通 MVP 功能**为先，选最简单能满足需求的实现，不做过度设计、不提前抽象、不为想象中的未来需求预留复杂结构。
- 单用户、本地、低频系统——避免分布式、缓存、插件化、复杂并发等与当前规模不匹配的方案。
- 如果某个需求的合理实现**确实比较复杂**（涉及架构改动、多模块联动、明显的性能/一致性权衡），**先停下来和用户确认**，讲清复杂点和更简单的替代方案，由用户决定，不要自行铺开。
- **不用数据库事务**：单用户、本地、低频，不为多写操作套 `Transaction`/回滚。多步写入按顺序直接写，中途出错就 fail-fast 报错退出、下次重跑，不追求跨步骤的原子一致性。如某处确有强一致需求，先和用户确认。



## 3. 其它既有约定

- 代码风格 fail-fast：暴露问题而非掩盖，尤其单测；不乱加兜底 fallback。
- 展示列表优先行内编辑，见 `[.cursor/rules/list-inline-edit.mdc](.cursor/rules/list-inline-edit.mdc)`。
- 写组件/代码前优先复用已有官方包和仓库内已有实现。

